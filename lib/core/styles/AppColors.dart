import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF2C3E50);
  static const Color secondary = Colors.blue;
  static const Color background = Color(0xFFF4F5F7);

  // This surface color is used for primary cards and page sections.
  static const Color surface = Color(0xFFFFFFFF);

  // This muted surface color is used for soft containers and secondary sections.
  static const Color surfaceMuted = Color(0xFFF0F2F5);

  // This elevated surface color is used for desktop cards with subtle contrast.
  static const Color surfaceElevated = Color(0xFFFFFFFF);

  // This sidebar background is used for desktop navigation shells.
  static const Color sidebarBackground = Color(0xFFF8F9FB);

  // This divider color is used for subtle separators on web layouts.
  static const Color divider = Color(0xFFE3E8EF);

  // This hover color is used for lightweight interactive desktop states.
  static const Color hover = Color(0xFFF1F3F7);

  // This overlay color is used for dimmed backgrounds and inactive layers.
  static const Color overlay = Color(0x14000000);

  // Text Colors
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Colors.blueGrey;
  static const Color textLight = Colors.grey;

  // This textMuted color is used for supportive text on desktop sections.
  static const Color textMuted = Color(0xFF6B7280);

  // This textSoft color is used for secondary labels in dense portal cards.
  static const Color textSoft = Color(0xFF475569);

  // Status Colors
  static const Color success = Colors.green;
  static const Color error = Colors.redAccent;

  // This warning color is used for attention or pending states.
  static const Color warning = Color(0xFFF59E0B);

  // This info color is used for neutral informational states.
  static const Color info = Color(0xFF2563EB);

  // UI Colors
  static const Color white = Colors.white;
  static const Color border = Color(0xFFCFD8DC); // blueGrey.shade200
  static const Color shadow = Colors.black12;

  // This cardBorder color is used for desktop cards and elevated sections.
  static const Color cardBorder = Color(0xFFE6EAF0);

  // This softShadow color is used for subtle desktop web elevation.
  static const Color softShadow = Color(0x12000000);

  // Icon Colors
  static const Color iconGrey = Colors.grey;
}